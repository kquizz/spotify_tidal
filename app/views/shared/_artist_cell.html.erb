<div class="card">
  <div class="card-body">
    <h3 class="text-lg font-semibold"><%= artist.name %></h3>
    <p class="text-sm text-orange-600"><%= artist.albums.count %> albums • <%= artist.songs.count %> songs</p>
    <%= link_to "View Albums", artist_path(artist), class: "btn btn-sm btn-primary mt-2" %>
    <%= button_to "Unsave", artist_path(artist), method: :delete, class: "btn btn-sm btn-danger mt-2 ml-2", data: { confirm: "Are you sure?" } %>
  </div>
</div>